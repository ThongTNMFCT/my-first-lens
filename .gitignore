# --- CẤU HÌNH (QUAN TRỌNG) ---
# Đã thêm dấu # ở đầu để CHO PHÉP upload file này lên GitHub
# Vercel cần file này để web hoạt động
# config.js

# Các file môi trường khác (nếu có) thì vẫn nên ẩn
.env

# --- File hệ thống (Rác của Windows/Mac) ---
.DS_Store
Thumbs.db
desktop.ini

# --- IDE & Editor (Cấu hình riêng của VS Code...) ---
.vscode/
.idea/
*.sublime-workspace

# --- Logs & Debug (File báo lỗi) ---
*.log
npm-debug.log*
yarn-debug.log*
firebase-debug.log
firebase-debug.log.*

# --- Firebase Cache ---
.firebase/

# --- Dependencies (Thư viện) ---
node_modules/
dist/
build/